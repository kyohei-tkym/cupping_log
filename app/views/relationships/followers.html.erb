<div class="container">
  <h2 class="text-center pt-5">Follower</h2>
  <% if @users.exists? %>
    <%= render 'search/search-user-index', datas: @users %>
  <% else %>
    <p>ユーザーはいません</p>
  <% end %>
</div>