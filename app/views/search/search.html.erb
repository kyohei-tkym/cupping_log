<div class="container">
    <% if @model == "user" %>
    <h2>'<%= @value %>'に一致するユーザー</h2>
    <table class="table">
        <thead>
            <tr>
                <th></th>
                <th>ユーザー名</th>
                <th>自己紹介</th>
            </tr>
        </thead>
    <% @datas.each do |user| %>
        <tbody>
            <tr>
                <th>
                    <%= attachment_image_tag(user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg", size:'40x40') %>
                </th>
                <th>
                    <%= user.name %>
                </th>
                <th>
                    <%= user.introduction %>
                </th>
            </tr>
        </tbody>
    <% end %>
    <% elsif @model == "log" %>
    <h2>'<%= @value %>'に一致するLog</h2>
    <%= render 'logs/log-index', logs: @datas %>
    </table>
  </div>
</div>